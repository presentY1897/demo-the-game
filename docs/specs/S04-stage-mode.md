# S04. Symposia 스테이지 모드

- **갈래**: UX · **의존**: 없음 (S02와 병행 가능) · **판정**: 대기

## 배경 (진단 근거)

Symposia의 실제 사용 맥락은 **어두운 강연장에서 폰으로 30–60분 시청**이다. 그런데
`theme.ts`가 `semanticColor.light` 고정이라 다크 모드가 없고(tokens의 다크 세트는
정의만 있고 미사용), 화면 꺼짐 방지가 없어 몇 분이면 자막이 꺼진다.

## 명세

### 테마 전환 기반

- `theme.ts`의 정적 객체를 테마 컨텍스트(light/dark)로 교체 — `semanticColor[mode]` 소비.
  기존 화면의 `color.*` 참조는 훅(`useTheme()`) 경유로 전환.
- 시스템 다크 설정(`useColorScheme`)을 기본값으로 따르되, 스테이지 모드는 강제 다크.

### 스테이지 모드 (자막 화면 토글)

- 토글 ON 시:
  - 다크 테마 강제 + 상태바 어둡게.
  - **최신 확정 자막 1건 + 진행 중 부분 자막**만 대형 타이포로 표시(히스토리 리스트 숨김).
    번역 병기 유지, 폰트 조절(A−/A＋)은 스테이지 모드에서도 동작.
  - `expo-keep-awake` 활성 — 시청 중 화면이 꺼지지 않는다. OFF/화면 이탈 시 해제.
- 토글 OFF 시 기존 히스토리 리스트 뷰로 복귀(스크롤 위치·데이터 보존 — 스토어는 그대로,
  뷰만 전환).

### 대비 기준

- 다크 팔레트의 자막 텍스트/배경 대비는 WCAG AA(4.5:1) 이상 — S06의 대비 검사 스크립트로
  검증(토큰 값 조정이 필요하면 tokens에서 수정).

## 완성 기준

1. 스테이지 모드 토글 왕복 시 데이터 손실 없이 뷰가 전환된다.
2. 스테이지 모드 중 화면이 자동으로 꺼지지 않는다(keep-awake 활성 확인).
3. 다크 상태에서 자막 대비 AA 충족.
4. 시스템 다크 설정 시 앱 전체가 다크로 뜬다.

## 테스트

- 유닛: 테마 컨텍스트 — 모드별 토큰 매핑, 시스템 다크 추종, 스테이지 모드 강제 다크 우선.
- 유닛: 스테이지 뷰 셀렉터 — "최신 확정 1건 + 진행 중 부분 자막"만 선택됨(스토어 레벨).
- 자동(대비): 다크 팔레트 AA는 S06의 tokens 대비 테스트가 커버.
- 수동: 토글 왕복 시 데이터·스크롤 보존, 화면 자동 잠금을 30초로 설정하고 keep-awake 확인,
  스테이지 모드 중 폰트 조절 동작.

## 작업 분해

1. 테마 컨텍스트 도입 + 기존 화면 마이그레이션
2. 스테이지 모드 뷰(대형 자막) + 토글
3. expo-keep-awake 연동 (설치 필요)
4. 다크 대비 확인·토큰 조정

## 범위 제외

수동 라이트/다크 설정 화면(시스템 추종 + 스테이지 강제 다크로 충분), product 홈페이지
다크 모드(별도 판단), 가로 모드 전용 레이아웃.

## 구현 메모 (2026-09-01)

명세와 달라진/구체화된 지점:

- **뷰 전환은 언마운트가 아니라 오버레이**. 히스토리 `FlatList`는 마운트된 채 두고
  스테이지 뷰를 `position: absolute`로 덮는다 — 토글 왕복 시 스크롤 위치가 보존돼야
  하는데(완성 기준 1) 언마운트하면 잃기 때문. 가려진 리스트는
  `importantForAccessibility="no-hide-descendants"`로 스크린리더에서도 숨긴다.
- **화면 이탈 시 스테이지 모드 자동 해제**. keep-awake뿐 아니라 강제 다크도
  자막 화면 전용이라, `SymposiaScreen` 언마운트에서 스토어를 끈다.
- **테마는 컨텍스트 + 모드별 StyleSheet 캐시**. RN `StyleSheet`가 정적이라
  `createThemedStyles(color => ...)`로 시트를 light/dark 각각 한 번만 만들어 캐시하고,
  `useThemedStyles()`가 현재 모드의 시트를 고른다(렌더마다 스타일 객체를 새로 만들지 않음).
- `app.json`의 `userInterfaceStyle`을 `light` → `automatic`으로 바꿔야
  네이티브에서 `useColorScheme()`이 OS 설정을 실제로 돌려준다.

### 다크 대비 측정 (WCAG 2.1, 자막 화면에서 쓰는 조합만)

| 조합 | 색 | 대비 | 기준 |
|---|---|---|---|
| 확정 자막 원문 `text`/`bg` | #F7F9F9 on #0D1514 | **17.52:1** | 4.5 |
| 번역문 `primary`/`bg` | #38BAB5 on #0D1514 | **7.81:1** | 4.5 |
| 진행 중 부분 자막 `textMuted`/`bg` | #93A1A1 on #0D1514 | **6.92:1** | 4.5 |
| 세션 제목·헤더 `text`/`bg` | #F7F9F9 on #0D1514 | **17.52:1** | 4.5 |
| 발표자·세션 종료 안내 `textMuted`/`bg` | #93A1A1 on #0D1514 | **6.92:1** | 4.5 |
| A−/A＋ `text`/`surfaceSubtle` | #F7F9F9 on #263030 | **12.84:1** | 4.5 |
| 비활성 언어칩 `textMuted`/`surfaceSubtle` | #93A1A1 on #263030 | **5.07:1** | 4.5 |
| 활성 칩·계속보기 `onPrimary`/`primary` | #16201F on #38BAB5 | **7.03:1** | 4.5 |
| 재시도 `primary`/`surfaceSubtle` | #38BAB5 on #263030 | **5.72:1** | 4.5 |
| 연결 끊김 `danger`/`surfaceSubtle` | #F87171 on #263030 | **4.90:1** | 4.5 |
| 배지 open/connecting/closed/idle | 각 tone on tone 12% 합성 | **4.86 / 5.04 / 5.73 / 5.79:1** | 4.5 |

- **토큰 조정**: 다크 `danger`가 `red[500]`(#DC2626)일 때 `surfaceSubtle` 대비 **2.81:1**로
  AA 미달이라 `red[400]`(#F87171)을 팔레트에 추가해 다크 세트만 교체했다(4.90:1).
  라이트 세트는 손대지 않았다.
- **남은 미달(범위 밖)**: 헤더 구분선 `border`/`bg`는 다크 1.92:1 · 라이트 1.29:1로
  비텍스트 3:1 기준에 미달한다. 라이트도 같은 값이라 의도된 장식용 구분선으로 보고
  이번 범위에서는 건드리지 않았다 — S06 대비 검사에서 판단 대상.
